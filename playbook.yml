---
- hosts: onos-build-machine
  sudo: yes
  vars_files:
    - group_vars/config.yml
  roles:
    - apt
    - ntp
    - hosts
    - java
    - maven
    - karaf
    - onos_env
    - onos
    - cell
    - help
